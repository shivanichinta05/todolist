<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo List</title>
    <style>
    @import url("https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600&display=swap");

    *,
    *::after,
    *::before {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    body {
        font: 16px/1.2 "Montserrat", sans-serif;
        background: beige;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
    }

    .container {
        text-align: center;
    }

    h1 {
        font-size: 3rem; /* Increased font size */
        margin-bottom: 20px;
        color: #333;
    }

    form {
        margin-bottom: 20px;
    }

    input[type="text"] {
        width: 300px;
        height: 40px;
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 5px;
        font-size: 16px;
    }

    button {
        height: 40px;
        padding: 0 15px;
        margin-left: 10px;
        border: none;
        border-radius: 5px;
        background-color: #4f7196;
        color: #fff;
        font-size: 16px;
        cursor: pointer;
        transition: background-color 0.3s;
    }

    button:hover {
        background-color: #4089c9;
    }

    .wrapper {
        display: grid;
        grid-template-columns: repeat(3, 1fr); /* Ensure three columns */
        gap: 1rem;
        justify-items: center;
    }

    .todo__item {
        background-color: pink;
        border-radius: 15px;
        box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        padding: 0.3rem; /* Further reduced padding */
        width: 100px; /* Reduced width */
        text-align: center;
        transition: transform 0.2s;
        position: relative;
    }

    .todo__item:hover {
        transform: scale(1.05);
    }

    .todo__item p {
        color: navy;
        font-size: 0.9rem; /* Smaller font size */
        margin-bottom: 0.5rem; /* Reduced margin */
    }

    .todo__actions {
        display: flex;
        justify-content: space-around;
        align-items: center;
        flex-wrap: wrap;
        opacity: 0;
        transition: opacity 0.3s;
    }

    .todo__item:hover .todo__actions {
        opacity: 1;
    }

    .todo__actions a {
        text-decoration: none;
        color: #fff;
        background-color: #90ce9e;
        padding: 0.2rem 0.4rem; /* Smaller padding */
        border-radius: 5px;
        margin: 0.1rem; /* Reduced margin */
        font-size: 0.8rem; /* Smaller font size for actions */
        transition: background-color 0.3s;
    }

    .todo__actions a:hover {
        background-color: #218838;
    }

    .todo__actions .edit {
        background-color: skyblue;
    }

    .todo__actions .edit:hover {
        background-color: #0056b3;
    }
    </style>
</head>
<body>
    <div class="container">
        <h1>Todo List</h1>
        <form action="/" method="post" autocomplete="off">
            <input type="text" placeholder="Add todo's here" name="Todo" required>
            <button type="submit">Add</button>
        </form>
        <% if (data) { %>
        <div class="wrapper">
            <% data.forEach(Todo => { %>
            <div class="todo__item">
                <p><%= Todo.todo %></p>
                <div class="todo__actions">
                    <a href="/update/<%= Todo.id %>" class="edit">✏️</a>
                    <a href="/delete/<%= Todo.id %>" class="delete">delete</a>
                </div>
            </div>
            <% }) %>
        </div>
        <% } %>
    </div>
</body>
</html>
